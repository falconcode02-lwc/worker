package io.falconFlow.DSL.workflow.helpers;

public enum ActivityType {
  start,
  activity,
  function,
  wait,
  condition,
    controller,
    signal,
    switches,
    aiagent,
    plugin,
    childwf
}
