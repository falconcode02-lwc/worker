spring.profiles.active=dev,mysql,cache-redis

# --- Secret vault configuration ---
# Vault configs are in props-dev/vault/ and props-prod/vault/
# Supported vault types: DB (default), AZURE, GCP
# Configure via environment variables or profile-specific properties